"use client";

import type { AccountUser } from "./PrintPilotClient";

export default function AccountPanel({ user, inventoryCount, onClose }: { user: AccountUser; inventoryCount: number; onClose: () => void }) {
  return <div className="drawer-backdrop" onMouseDown={onClose}><aside className="account-drawer" onMouseDown={event => event.stopPropagation()} role="dialog" aria-modal="true"><div className="drawer-head"><div><span className="eyebrow">MON COMPTE</span><h2>{user ? "Profil PrintPilot" : "Connexion requise"}</h2></div><button className="icon-button" onClick={onClose}>×</button></div>{user ? <><div className="account-identity"><span>{user.displayName.charAt(0).toUpperCase()}</span><div><b>{user.displayName}</b><small>{user.email}</small></div></div><div className="account-stats"><div><b>{inventoryCount}</b><span>bobines enregistrées</span></div><div><b>Creality Hi</b><span>imprimante principale</span></div><div><b>0,4 mm</b><span>buse par défaut</span></div></div><div className="account-note"><b>Données personnelles</b><p>L’inventaire et l’historique sont isolés par adresse Google. Google ne reçoit aucun STL ni réglage d’impression.</p></div><a className="secondary full account-link" href="/auth/logout">Se déconnecter</a></> : <section className="auth-gate account-auth-gate"><span>SESSION FERMÉE</span><h3>Tu n’es pas connecté</h3><p>Connecte-toi avec Google pour ouvrir ton inventaire et ton historique. Aucun faux profil n’est créé à ta place.</p><a className="primary account-link" href="/auth/google?return_to=%2F">Se connecter avec Google</a></section>}</aside></div>;
}
